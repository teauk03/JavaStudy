--  참조 변수 super.
* 객체 자신을 가르치는 참조 변수이며, 인스턴스 메서드(생성자) 안에서만 사용할 수 있음
* Static 메서드 내에서는 사용 불가능..
* 상위 클래스의 멤버와 자손 클래스의 멤버를 구분하기 위해 사용한다.
    * this 키워드와 매우 흡사함.
        * lv(로컬 변수) iv(인스턴스 변수) 구별에 사용했다.
    * super 은 상위 멤버와 하위 멤버를 구별하는데 사용한다.
