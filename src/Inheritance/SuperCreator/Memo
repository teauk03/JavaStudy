- super() : 상위 클래스의 생성자
    * 상위 클래스의 생성자를 호출할 때 사용한다.
    * 상위 클래스의 멤버는 상위 클래스의 생성자를 호출해서 초기화 해야한다.
    * 자신 클래스의 멤버를 초기화 할때는 iv를 초기화 하기위한 this 키워드를 사용한다.
    * 상위 클래스의 멤버를 초기화 할때는 super() 키워드를 사용한다.

    - super() 키워드의 조건
        * 생성자의 첫 줄에 반드시 생성자를 호출 해야한다.